import { Desc, Title } from "@/constant/constant";

export const metadata = {
  title: `One Piece Episode 1024 Watch Online - ${Title}`,
  description: Desc,
};
export default function layout({ children }) {
  return <>{children}</>;
}
